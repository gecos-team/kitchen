%table.full.tablesorter
  %thead
    %th Name
    %th Members
  %body
    - roles.each do |role|
      %tr
        %td= link_to role.name.gsub('_',' '), role_path(role.name)
        %td= role.nodes.size
