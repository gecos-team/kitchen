= javascript_include_tag :users, :advanced_data
#attr_base
  - @skel.each do |attribute|
    = raw render_base_attribute(attribute, "[role][default_attributes]", nil,1)

= form_tag role_path(@role.name), :method => :put, :id => "edit_role_attributes", :remote => true do
  - @skel.each do |attribute|
    = raw render_fieldset(attribute, @data[attribute[0]], "[role][default_attributes]", @defaults, @use_default_data,nil,1)


  = submit_tag

  #default.float_right
    = check_box "role[default_attributes][default]", params[:recipe], :checked => ("checked" if !@use_default_data.blank?), :class => @input_class
    Use default values
